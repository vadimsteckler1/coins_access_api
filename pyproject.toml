[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "lumache"
authors = [{name = "Vadim Steckler", email = "vadim.steckler@coins.ph"}]
dynamic = ["version", "description"]
